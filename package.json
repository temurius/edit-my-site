{
  "name": "edit-my-site-ai",
  "private": true,
  "version": "0.1.0",
  "description": "Edit-My-Site AI monorepo (Angular client + Express server)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "concurrently -k \"npm:client start\" \"npm:server dev\"",
    "start:client": "npm --workspace client run start",
    "start:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "format": "prettier --write .",
    "lint": "echo 'Linting placeholder. Configure ESLint in client/server.'"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "prettier": "^3.3.3"
  }
}

